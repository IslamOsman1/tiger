{
  "name": "tiger-textile-printing",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack website and admin dashboard for textile printing using React, Vite, Tailwind, Express, MongoDB and Cloudinary.",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
